import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shishir.blooddonors',
  appName: 'Shishir Blood Donors',
  webDir: 'dist'
};

export default config;
